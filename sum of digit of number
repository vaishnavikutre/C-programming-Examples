#include <stdio.h>

int main() {
   int n,sum=0,reminder;
   printf("Enter number\n");
   scanf("%d",&n);
   while(n!=0)
   {
       reminder=n%10;
       sum+=reminder;
       n=n/10;
   }
    printf("%d",sum);
    return 0;
}
