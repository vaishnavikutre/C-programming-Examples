#include <stdio.h>

int main() {
   int n,reminder,rev=0;
   printf("Enter Number\n");
   scanf("%d",&n);
   while(n!=0)
   {
       reminder=n%10;
       rev=rev*10+reminder;
       n=n/10;
   }
    printf("%d",rev);
    return 0;
}
