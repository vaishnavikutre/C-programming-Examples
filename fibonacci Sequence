#include<stdio.h>
int main(){
    int n,x=0,y=1,z,i=1;
    printf("Enter the number of terms\n");
    scanf("%d",&n);
    
    printf("%d ",y);
    
    while(i<n)
    {
        z=x+y;
        x=y;
        y=z;
        printf("%d ",z);
        i++;
    }
    return 0;
}
